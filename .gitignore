out/
dist/
node_modules/
coverage/
npm-debug.log
yarn-error.log
app/node_modules/
vendor/windows-argv-parser/build/
vendor/desktop-notifications/build/
vendor/desktop-trampoline/build/
.DS_Store
.awcache
.idea/
.vs/
.vscode/*.log
.eslintcache
*.iml
.envrc
junit*.xml
*.swp
tslint-rules/
playwright-videos/
.cache/
.serena/

# Local agent / harness caches (added by Mavis / Serena / cortext-harness
# running against this repo). Safe to ignore — regenerable from tooling state.
.cortext-harness/
.scratch/
dist-debug/

# Generated build metadata — produced by clangd / VS Code's "Compile Database"
# feature. Not source-of-truth; regenerate with `bear` or your editor.
compile_commands.json

# pnpm is not used by this project (yarn only). The presence of pnpm-lock.yaml
# alongside yarn.lock usually means a stray editor/IDE accidentally picked the
# wrong package manager.
pnpm-lock.yaml

